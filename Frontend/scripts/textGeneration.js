/* 
	category: string, what type of thing to generate
	options: {optimism: int 0-10, genres: [{name: string, amount: int 0-10}}]
	returns: string of the text
*/
function generateText(category, options) {
	return 'todo';
}
export default generateText;
